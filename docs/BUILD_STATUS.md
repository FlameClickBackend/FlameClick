# FlameClick V1 Build Status

## Current milestone

The real application repository has been bootstrapped on `build/v1-bootstrap`.

Foundation now present:

- Next.js App Router
- TypeScript strict mode
- React
- FlameClick root layout and metadata
- FlameClick V1 visual/design foundation
- Initial real landing experience
- Mobile responsive baseline
- Environment and build output ignore rules

## Product contracts

The build is governed by three V1 specifications:

1. Customer nine-stage chain
2. Owner/Admin control plane
3. Infrastructure & Security foundation

Customer chain:

Visitor → Discovery → Account → Brand Context → Campaign Intelligence → Questioning → Blueprint → Delivery → Repeat Use

## Next build work

1. Transfer the locked prototype experience into reusable React components.
2. Establish shared application shell and design tokens.
3. Add customer route structure.
4. Add owner/admin route structure.
5. Add test and CI foundation.
6. Connect deployment preview.
7. Add Supabase only after the UI foundation is verified.

No production secrets belong in this repository.
